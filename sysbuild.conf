# SPDX-License-Identifier: Apache-2.0
#
# Sysbuild (multi-image) configuration.
# MCUboot is available in the workspace (bootloader/mcuboot) but disabled
# by default. Enable it here or with -DSB_CONFIG_BOOTLOADER_MCUBOOT=y.

SB_CONFIG_BOOTLOADER_MCUBOOT=n
